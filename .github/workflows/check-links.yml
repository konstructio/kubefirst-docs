name: Markdown Links Validation

on: push

jobs:
  markdown-link-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3.3.0
    - uses: gaurav-nelson/github-action-markdown-link-check@v1.0.13
      with:
        config-file: 'check_links_config.json'