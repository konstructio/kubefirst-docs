## Connecting to Kubernetes

To connect to your new Kubernetes cluster, run

```shell
export KUBECONFIG=~/.k1/kubefirst/kubeconfig
```

To view all cluster pods, run

```shell
kubectl get pods -A
```
